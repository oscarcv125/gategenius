# GateGenius

A comprehensive facility management and access control dashboard.

## Technical Details
- **Frontend**: React, Zustand (State Management), Tailwind CSS, Recharts (Data Visualization).
- **Backend**: Express, MySQL2, JWT Authentication, bcryptjs.
- **Features**: Generates PDF reports (`jspdf`) and parses CSV data (`papaparse`).